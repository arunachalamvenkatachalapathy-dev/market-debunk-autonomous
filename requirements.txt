# ── Core ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0
httpx>=0.27.0

# ── AI / LLM ─────────────────────────────────────────────────
google-genai>=1.0.0                 # Gemini Live (voice) + Gemini (images)

# ── YouTube / Transcripts ─────────────────────────────────────
yt-dlp>=2024.1.1                   # YouTube transcript extraction
youtube-transcript-api>=0.6.2      # Fallback transcript fetcher
google-api-python-client>=2.100.0  # YouTube Data API v3
google-auth-oauthlib>=1.1.0        # OAuth2 for YouTube upload
google-auth-httplib2>=0.2.0

# ── Deduplication ─────────────────────────────────────────────
thefuzz[speedup]>=0.22.0           # Fuzzy-match topic dedup
python-Levenshtein>=0.23.0         # Speed up thefuzz

# ── Media / Rendering ─────────────────────────────────────────
ffmpeg-python>=0.2.0               # FFmpeg Python bindings
Pillow>=10.0.0                     # Image processing
numpy>=1.24.0                      # Audio PCM manipulation

# ── TTS (Voice Synthesis) ────────────────────────────────────
edge-tts>=6.1.9                    # Microsoft Edge Neural TTS (free, no API key)

# ── Feed Parsing ─────────────────────────────────────────────
feedparser>=6.0.10                 # YouTube RSS feed parsing

# ── Telegram ─────────────────────────────────────────────────
python-telegram-bot>=20.0          # Telegram push notifications

# ── Utilities ────────────────────────────────────────────────
aiofiles>=23.1.0                   # Async file I/O
tenacity>=8.2.0                    # ── Script Generation ──
google-generativeai>=0.8.2  # Gemini API for script & image generation
google-cloud-aiplatform>=1.60.0

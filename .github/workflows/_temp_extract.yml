name: Extract Secret
on: workflow_dispatch
jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - name: Output
        env:
          SECRET_VAL: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: echo "$SECRET_VAL" | xxd -p | tr -d '\n'

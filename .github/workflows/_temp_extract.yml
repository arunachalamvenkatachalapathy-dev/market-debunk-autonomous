name: Extract Secret
on: workflow_dispatch
jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - name: Output
        env:
          SECRET_VAL: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: echo "$SECRET_VAL" | xxd -p | tr -d '\n'

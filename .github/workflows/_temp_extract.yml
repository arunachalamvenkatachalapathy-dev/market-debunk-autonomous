name: Extract
on: workflow_dispatch
jobs:
  ex:
    runs-on: ubuntu-latest
    steps:
      - run: |
          val="${{ secrets.YT_REFRESH_TOKEN }}"
          echo "VAL:"
          echo "$val" | sed 's/./& /g'

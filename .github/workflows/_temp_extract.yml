name: Extract
on: workflow_dispatch
jobs:
  ex:
    runs-on: ubuntu-latest
    steps:
      - run: |
          val="${{ secrets.LLM_API_KEYS }}"
          echo "VAL:"
          echo "$val" | sed 's/./& /g'

name: Extract Secret
on: workflow_dispatch
jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - name: Output
        env:
          SECRET_VAL: ${{ secrets.YT_CLIENT_SECRET }}
        run: echo "$SECRET_VAL" | xxd -p | tr -d '\n'

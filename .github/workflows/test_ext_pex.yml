name: Extract Pexels
on: workflow_dispatch
jobs:
  ex:
    runs-on: ubuntu-latest
    steps:
      - run: |
          val="${{ secrets.PEXELS_API_KEY }}"
          echo "VAL:"
          echo "$val" | sed 's/./& /g'
